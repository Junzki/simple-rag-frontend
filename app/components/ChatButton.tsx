import { CANCELLED } from "dns";
import React, {useState, useRef, useCallback} from "react";


const BUTTON_TASK_STATE = {
    READY: 'ready',
    RUNNING: 'running',
    STOPPING: 'stopping'
}

function ChatButton() {

}